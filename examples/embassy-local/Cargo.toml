[package]
name = "embassy-local"
authors = ["nostalgia <lv15623448685@gmail.com>"]
edition.workspace = true

[dependencies]
axstd = { workspace = true, features = ["alloc", "multitask"], optional = true }
axasync = { workspace = true, features = ["thread"] }

embassy-executor = { git = "https://github.com/embassy-rs/embassy", branch = "main", default-features = false }
embassy-time = { git = "https://github.com/embassy-rs/embassy", branch = "main" }
