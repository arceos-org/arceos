#include <stdio.h>
#include <sys/types.h>

// TODO:
int fchmod(int fd, mode_t mode)
{
    unimplemented();
    return 0;
}

// TODO:
int mkdir(const char *pathname, mode_t mode)
{
    unimplemented();
    return 0;
}
